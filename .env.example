# Frontend (Vite) - copy to .env.local and fill in
# See docs: https://docs.convex.dev/production/environment-variables

# Convex deployment URL (from Convex dashboard or `npx convex dev`)
VITE_CONVEX_URL=

# Clerk publishable key (from Clerk dashboard > API Keys)
VITE_CLERK_PUBLISHABLE_KEY=

# Backend (Convex) - set in Convex Dashboard (Settings > Environment Variables):
# CLERK_ISSUER_URL - from Clerk dashboard (e.g. https://your-app.clerk.accounts.dev)
# GOOGLE_GENERATIVE_AI_API_KEY - for chat/Gemini (required for /api/chat)
# CLIENT_ORIGIN - frontend URL(s) for CORS, comma-separated (e.g. https://your-app.vercel.app). No trailing slashes.
# MCP_SERVER_URL - optional, for MCP tools in chat
# NEO4J_URI - optional, for knowledge graph queries
# NEO4J_USERNAME - optional, for knowledge graph queries
# NEO4J_PASSWORD - optional, for knowledge graph queries
